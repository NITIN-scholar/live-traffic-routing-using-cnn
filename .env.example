# Optional settings for online setup. Keep real keys in .env and never commit them.
# The current V1 runs locally with cached/sample data and does not require API keys.
APP_ENV=local
OSMNX_PLACE_NAME=Dehradun, Uttarakhand, India
OFFLINE_MODE=true
